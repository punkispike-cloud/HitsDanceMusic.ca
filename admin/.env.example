# URL publique de l'API (service Railway `api`).
# En prod : https://api.hitsdancemusic.ca
# En dev local : http://localhost:8082
NEXT_PUBLIC_API_URL=http://localhost:8082

# Sentry (monitoring d'erreurs navigateur) — laisser vide pour désactiver.
# Créer un projet Sentry distinct de l'API (DSN admin ≠ DSN api).
NEXT_PUBLIC_SENTRY_DSN=
